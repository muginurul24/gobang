global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/onixggr/alerts.rules.yml

scrape_configs:
  - job_name: onixggr
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:9090
